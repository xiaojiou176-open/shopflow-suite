import { createAppConfig } from '../../tooling/wxt/create-app-config';

export default createAppConfig({
  name: 'Shopflow Suite',
  description:
    'Internal-only alpha composition shell for capability navigation and rollout visibility.',
  hostPermissions: [],
});
